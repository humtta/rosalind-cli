# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: "3"

tasks:
  build: go build -o ./out/rosalind ./cmd/rosalind/
  run: go run ./cmd/rosalind/
  format: go fmt ./...
  lint: go vet ./...
  check:
    cmds:
      - task format
      - task lint
    silent: true
